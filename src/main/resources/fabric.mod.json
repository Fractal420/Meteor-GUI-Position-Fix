{
  "schemaVersion": 1,
  "id": "meteor-gui-position-fix",
  "version": "${version}",
  "name": "Meteor GUI Position Fix",
  "description": "Keeps Meteor ClickGUI window positions stable, prevents search resizing and category reflow, snaps only the dragged window on release, prioritizes expanded setting popups, and supports custom themes.",
  "authors": [
    "OpenAI"
  ],
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "meteor": [
      "com.meteorfix.gui.MeteorGuiPositionFix"
    ]
  },
  "mixins": [
    {
      "config": "meteor-gui-position-fix.client.mixins.json",
      "environment": "client"
    }
  ],
  "depends": {
    "minecraft": "~1.21.11",
    "fabricloader": ">=0.19.5",
    "meteor-client": "*"
  }
}
